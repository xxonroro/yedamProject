<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yedam.member.MemberMapper">
	<insert id="memberInsert" parameterType="com.yedam.member.UserVO">
		INSERT INTO users (user_id, password, name, address, phone, birthdate)
		VALUES (#{userId}, #{upassword}, #{uname}, #{address}, #{phone}, #{birthdate})
	</insert>
	
	<select id="checkMemberId" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM users
		WHERE user_id = #{id}
	</select>
	
	<select id="checkMemberPhone" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM users
		WHERE phone = #{phone}
	</select>
	
	
</mapper>